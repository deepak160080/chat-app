import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primaryColor = Color(0xFF5953FF);
  static const Color primaryLightColor = Color(0xFF7A76FF);
  static const Color primaryDarkColor = Color(0xFF3D35E0);

  // Background colors
  static const Color backgroundColor = Color(0xFF121212);
  static const Color surfaceColor = Color(0xFF262630);

  // Text colors
  static const Color primaryTextColor = Colors.white;
  static const Color secondaryTextColor = Colors.grey;

  // Action colors
  static const Color actionColor = Color(0xFF5953FF);
  static const Color errorColor = Colors.red;
  static const Color successColor = Colors.green;

  // Other colors
  static const Color dividerColor = Colors.grey;
  static const Color iconColor = Colors.white;
}